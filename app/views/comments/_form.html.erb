<%= form_for([@event, @event.comments.build]) do |f| %>
  <p>
    <%= select("user", "userid", @users.collect {|u| [ u.name, u.id ] }) %>
  </p>
  <p>
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
